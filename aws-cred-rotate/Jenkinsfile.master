pipeline {
  agent {
    node {
      label "master"
    }
  }
  stages {
    stage("Rotate keys") {
      steps {
         withAWS( credentials: 'main-jenkins', region: 'us-west-2') {
           sh 'rotate_master.sh -i test2-jenkins -u test2-jenkins -d 0'
         }
      }
    }
  }
}
